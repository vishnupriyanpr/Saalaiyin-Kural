/** @type {import('next').NextConfig} */
const nextConfig = {
  // "/" serves the marketing landing (app/page.tsx). Visitors proceed to /login
  // from there via the "Enter Portal" CTA — no forced redirect.
};

export default nextConfig;
